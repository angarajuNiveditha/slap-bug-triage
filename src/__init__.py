"""
SLAP Bug Triage — source package.

Sub-packages:
  agents/     — multi-agent pipeline (host + sub-agents)
  ml/         — embedding classifier + similarity engine
  rule_based/ — regex/TF-IDF simulation pipeline
  shared/     — Jira client, DB, Claude CLI, Genvoy, ticket builder
  sdk/        — Anthropic SDK reference pipeline (requires ANTHROPIC_API_KEY)
"""
