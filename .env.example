# Copy this to .env and fill in your values
# NEVER commit .env to git

JIRA_EMAIL=angaraju.v@flipkart.com
JIRA_TOKEN=your_token_here
JIRA_BASE_URL=https://flipkart.atlassian.net
JIRA_PROJECT=FLIPPI

ANTHROPIC_API_KEY=your_claude_api_key_here

# Local ticket store (see src/db.py). SQLAlchemy DSN.
# Local MySQL default (`brew install mysql && brew services start mysql`):
DATABASE_URL=mysql+pymysql://root@localhost/slap_triage
# SQLite fallback (zero-setup, if MySQL isn't available):
# DATABASE_URL=sqlite:///data/tickets.db
